
function intersect(a1, b1, a2, b2) {
    if (a2 <= b1 & b2 >= a1){
        return 1;
    }
    return 0;
}

return intersect(1, 5, 1, 7) + intersect(1, 5, -10, 1) + intersect(1, 5, 6, 7) + intersect(0, 0, 0, 2) + intersect(0, 1, 2, 2) + intersect(2, 3, -1 ,0);